#include<iostream>
using namespace std;
int main()
{
    int front=-1,rare=-1,range,sel;
    cout<<"Enter the range : ";
    cin>>range;
    int arr[range];
    while(true)
    {
        cout<<"Enter the selector : ";
        cin>>sel;
        switch (sel)
        {
            case 0:
            if()
            cout<<"Enter the item to enqueue : ";
            cin>>
        }
    }
}